// Install app-framework
- Clone app-framework
- sudo npm install  --unsafe-perm=true
- npm run patch

// Install cordova
- Install Xcode
- Open console window in project folder
- sudo npm -g install cordova
- cordova create cordova
- copy latest build folder content to cordova/www

// Run browser
- cd cordova
- cordova platform add browser
- cordova run browser

// Run ios in simulator
- sudo npm -g install ios-deploy --unsafe-perm=true
- cordova platform add ios
- open cordova/platforms/ios/HelloCordova.xcodeproj in Xcode
- Select device, start simulator

// Run ios on own device
- Sign to Apple developer program - 99€ per year
- Select project > select target > General > Signing: Automatically manage signing
- Select own device, start

// Update cordova
cordova plugin save
cordova platform rm ios
cordova platform add ios
cordova run ios

// Run android on OSX
- Install Android Studio (default options, with virtual device and HAXM)

Error: Git working directory not clean. >> Commit changes in GitHub desktop

Update app framework on ios: sudo npm update --unsafe-perm=true
